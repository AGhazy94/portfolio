{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "outputStyle": "Rundown",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "H=\"$CLAUDE_PROJECT_DIR/.claude/hooks/no-recursive-grep.py\"; [ -f \"$H\" ] || exit 0; if command -v python3 >/dev/null 2>&1; then exec python3 \"$H\"; else exec /usr/bin/python3 \"$H\"; fi"
          }
        ]
      }
    ]
  }
}
