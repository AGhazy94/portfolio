# `.agents/rules/`

Reference docs listed in the Rule Index at the bottom of [AGENTS.md](../../AGENTS.md). Nothing
attaches them by glob — read the one your task lands in. One copy of each rule lives here; other
agents' files link to it rather than repeating it.

| Doc           | Read it when                                                            |
| ------------- | ----------------------------------------------------------------------- |
| [seo](seo.md) | head tags, JSON-LD, sitemap, robots, `llms.txt`, OG image, deploy flags |
